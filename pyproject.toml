[project]
name = "dotx"
version = "0.0.1"

[build-system]
requires = ["hatch"]
build-backend = "hatchling.build"

[project.scripts]
dotx = "cli:cli"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
