[project]
name = "dotfiles"
version = "0.1"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tools.setuptools.packages]
find = {}

[project.scripts]
dotfiles = "cli:cli"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
